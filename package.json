{
  "name": "back-tereverde",
  "version": "1.0.0",
  "description": "Backend server for the Terê Verde React app",
  "main": "serve.js",
  "type": "commonjs",
  "scripts": {
    "start": "node serve.js",
    "build-frontend": "cd frontend && npm install && npm run build",
    "postinstall": "node scripts/movePublicNested.js && cd frontend && npm install"
  },
  "dependencies": {
    "bcrypt": "^5.1.0",
    "express": "^4.18.3",
    "sqlite3": "^5.1.6"
  }
}
